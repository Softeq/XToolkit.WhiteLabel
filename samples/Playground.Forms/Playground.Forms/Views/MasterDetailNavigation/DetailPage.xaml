<?xml version="1.0" encoding="utf-8"?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:Playground.Forms.ViewModels.MasterDetailNavigation;assembly=Playground.Forms"
    x:Class="Playground.Forms.Views.MasterDetailNavigation.DetailPage"
    x:DataType="vm:DetailPageViewModel"
    BackgroundColor="{AppThemeBinding Light=White, Dark=Black}">

    <StackLayout
        VerticalOptions="Center"
        HorizontalOptions="Center">
        <Label
            Text="Swipe from left to right to open left menu" />
        <Button
            Command="{Binding ToMainPageCommand}"
            Text="Return to main page" />
    </StackLayout>

</ContentPage>