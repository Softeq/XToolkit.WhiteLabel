<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:dialogs="clr-namespace:Playground.Forms.ViewModels.Dialogs;assembly=Playground.Forms"
             x:Class="Playground.Forms.Views.Dialogs.DialogsRoot"
             x:DataType="dialogs:DialogsRootViewModel">
    <ContentPage.Content>
        <StackLayout>
            <Button
                Command="{Binding ShowModalScreenCommand}"
                Text="Show modal and wait until screen close" />
            <Button
                Command="{Binding ShowModalScreenWithoutWaitCommand}"
                Text="Show modal without wait" />
            <Label
                HorizontalOptions="Center"
                Text="{Binding DialogResult}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>