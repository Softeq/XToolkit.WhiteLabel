<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:Playground.Forms.ViewModels;assembly=Playground.Forms"
             x:Class="Playground.Forms.Views.MainPage"
             x:DataType="viewModels:MainPageViewModel"
             Title="{Binding Title}">
    <ContentPage.Content>
        <StackLayout>
            <Button
                Command="{Binding SimpleNavigationCommand}"
                Text="Simple Navigation" />
            <Button
                Command="{Binding MasterDetailNavigationCommand}"
                Text="Master Detail Navigation" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>